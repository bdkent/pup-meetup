# NYC Shih Tzu Meetup — the dedicated organizer account (100+ attendees per event;
# upcoming dates announced via posts/flyers). Corrected 2026-06-20 from
# `masontheshihtzu1` (an individual dog account / participant) to the real
# organizer handle `nycshihtzumeetup`, verified via web search.
# Demonstrates the `instagram` source type. Ingest skips it unless APIFY_TOKEN is
# set; with a token, posts are fetched -> classified -> extracted to occurrences.
id: nyc-shih-tzu-meetup
name: NYC Shih Tzu Meetup
breeds:
  - shih-tzu
metro: nyc
timezone: America/New_York
home_geo:
  lat: 40.7626
  lng: -73.9568
poll_interval: daily
sources:
  - type: instagram
    handle: nycshihtzumeetup
