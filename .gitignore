# Dependencies
node_modules/

# Build output
dist/
build/
.output/
.astro/

# Environment / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-error.log*

# Cache
.cache/

# OS / editor
.DS_Store
.vscode/
.idea/

# Generated pipeline data (regenerated by `npm run ingest`; events are a
# rebuildable projection, not a durable asset — the durable raw store lives
# elsewhere). Revisit commit-vs-regenerate when the CI pipeline is built.
data/events/*.json
data/geocache.json
